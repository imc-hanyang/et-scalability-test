apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
spec:
  type: ClusterIP
  # type: NodePort
  # type: LoadBalancer
  sessionAffinity: ClientIP
  selector:
    easytrack: dashboard
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      # nodePort: 30080